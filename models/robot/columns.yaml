# ===================================
#  List Column Definitions
# ===================================

columns:
    agent:
        label: Agent
        searchable: true

    directives_count:
        label: Number of Entries
        relation: directives_count
        relationCount: true
