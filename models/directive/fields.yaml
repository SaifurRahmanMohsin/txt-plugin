# ===================================
#  Form Field Definitions
# ===================================

fields:
    type:
        label: Type
        commentAbove: The type of directive.
        type: dropdown
        span: left

    data:
        label: Data
        commentAbove: A string for comment, a relative URL for other types
        placeholder: "/private"
        span: right

    new_line:
        label: Add New Line? (To end a section)
        type: checkbox