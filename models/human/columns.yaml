# ===================================
#  List Column Definitions
# ===================================

columns:

    attribution:
        label: Attribution
        searchable: true

    information:
        label: Information
        select: (SELECT group_concat(value SEPARATOR ', ') FROM mohsin_txt_information WHERE human_id = mohsin_txt_humans.id)